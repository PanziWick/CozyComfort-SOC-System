@model List<Distributor.Web.Models.InventoryDTO>

<h2>Distributor Inventory</h2>

<table class="table">
    <thead>
        <tr>
            <th>Blanket ID</th>
            <th>Quantity</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.BlanketId</td>
                <td>@item.Quantity</td>
            </tr>
        }
    </tbody>
</table>
